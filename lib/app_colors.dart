import 'package:flutter/material.dart';

/// فئة الألوان المستخدمة في التطبيق
class AppColors {
  // الألوان الأساسية
  static const Color primary = Color(0xFFF8A8C8);      // وردي فاتح رئيسي
  static const Color secondary = Color(0xFFFFD6E7);    // وردي أفتح
  static const Color tertiary = Color(0xFFFFB3D9);     // وردي متوسط

  // ألوان الخلفيات
  static const Color background = Color(0xFFFFF8FB);   // خلفية بيضاء مع لمسة وردية
  static const Color surfaceLight = Color(0xFFFFF0F7); // سطح فاتح جداً

  // ألوان النصوص
  static const Color textDark = Color(0xFF4A4A4A);     // نص داكن
  static const Color textLight = Color(0xFF8A8A8A);    // نص فاتح

  // ألوان الحالات
  static const Color online = Color(0xFF66BB6A);       // أخضر (متصل)
  static const Color offline = Color(0xFFBDBDBD);      // رمادي (غير متصل)
  static const Color warning = Color(0xFFFF9800);      // برتقالي (تحذير)
  static const Color error = Color(0xFFEF5350);        // أحمر (خطأ)

  // ألوان الزر
  static const Color buttonPrimary = Color(0xFFF8A8C8);
  static const Color buttonSecondary = Color(0xFFFFD6E7);

  // ألوان الظلال
  static const Color shadow = Color(0x1A000000);

  // الأبيض
  static const Color white = Colors.white;
}
